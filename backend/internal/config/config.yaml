app:
  port: "8080"
  host: "localhost"
  admin: "admin"
  pwd: "admin123"

db:
  port: 5432
  host: "localhost"
  table: "mytable"
  user: "dbuser"
  pwd: "dbpassword"

auth:
  jwtkey: "your-secret-jwt-key"
  jwtkeyexpirationtimeh: 72
  openaikey: "your-openai-api-key"

cors:
  alloworigins:
    - "http://localhost:3000"
  allowmethods:
    - "GET"
    - "POST"
    - "PUT"
  allowheaders:
    - "Content-Type"
    - "Authorization"
  allowcredentials: true
